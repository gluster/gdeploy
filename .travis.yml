language: python
python:
  - "2.7"
env:
  - GDEPLOY_TEMPLATES='$TRAVIS_BUILD_DIR/gdeploy'
  - ANSIBLE_LIBRARY='$TRAVIS_BUILD_DIR/gdeploy/modules'
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: nosetests
