---
- hosts: servers
  remote_user: root
  gather_facts: no

  tasks:

  - name: Reducing the attributes of the logical volume
    lv: action=reduce
        force=y
        vgname=vg1
        lvname=lv1
        nofsck=n
        resizefs=n
        extents=-1

  #
