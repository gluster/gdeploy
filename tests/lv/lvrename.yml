---
- hosts: servers
  remote_user: root
  gather_facts: no

  tasks:

  - name: Rename the attributes of the logical volume
    lv: action=rename
        vgname=vg1
        lvname=lv1
        new_name=lv2
  
