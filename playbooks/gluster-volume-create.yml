---
- hosts: master
  remote_user: root
  gather_facts: no

  tasks:
  - name: Creates a volume
    ansible.builtin.expect:
      command: gluster volume create {{ volname }} {% if disperse == 'yes' %} {% if disperse_count | int > 0  %} disperse-data {{ disperse_count }} {% endif %} redundancy {{ redundancy_count }}  {% else %} replica {{ replica_count }} {% if arbiter_count | int  > 0 %} arbiter {{ arbiter_count }}  {% endif %} {% endif %} transport tcp {{ mountpoints | replace('[','') | replace(']','') | replace(',','') }} force
      responses:
        (.*)This configuration is not optimal on most workloads(.*): "y"
#    volume:  action=create
#             volume="{{ volname }}"
#             bricks='{{ mountpoints }}'
#             hosts="{{ hosts }}"
#             transport="{{ transport }}"
#             replica_count="{{ replica_count }}"
#             arbiter_count="{{ arbiter_count }}"
#             disperse="{{ disperse }}"
#             disperse_count="{{ disperse_count }}"
#             redundancy_count="{{ redundancy_count }}"
#             force="{{ force }}"
